name: hdfview release build

# Triggers the workflow on demand
on:
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type: release or snapshot'
        type: choice
        required: true
        default: snapshot
        options:
          - release
          - snapshot
      use_tag4:
        description: 'HDF Release version tag and base name'
        type: string
        required: false
        default: snapshot
      use_tag5:
        description: 'HDF5 Release version tag'
        type: string
        required: false
        default: snapshot

permissions:
  contents: read
  packages: write

jobs:
  determine-version:
    runs-on: ubuntu-latest
    outputs:
      version_tag: ${{ steps.construct-tag.outputs.VERSION_TAG }}
      build_type: ${{ steps.validate-input.outputs.BUILD_TYPE }}
      hdf4_tag: ${{ steps.validate-input.outputs.HDF4_TAG }}
      hdf5_tag: ${{ steps.validate-input.outputs.HDF5_TAG }}
    steps:
    - name: Validate input
      id: validate-input
      env:
        TAG: ${{ inputs.build_type }}
        TAG4: ${{ inputs.use_tag4 }}
        TAG5: ${{ inputs.use_tag5 }}
      run: |
          if [[ "$TAG" != "release" && "$TAG" != "snapshot" ]]; then
            echo "Error: build_type must be either 'release' or 'snapshot', got: $TAG"
            exit 1
          fi
          echo "BUILD_TYPE=$TAG" >> $GITHUB_OUTPUT
          echo "HDF4_TAG=$TAG4" >> $GITHUB_OUTPUT
          echo "HDF5_TAG=$TAG5" >> $GITHUB_OUTPUT

    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Read VERSION file
      id: read-version
      run: |
        # Extract HDFVIEW_VERSION from VERSION file (ignoring comments)
        HDFVIEW_VERSION=$(grep "^HDFVIEW_VERSION=" VERSION | cut -d'=' -f2)
        echo "version=$HDFVIEW_VERSION" >> $GITHUB_OUTPUT

    - name: Extract version number
      id: extract-version
      run: |
        # Version is already numeric from HDFVIEW_VERSION property
        VERSION_BASE="${{ steps.read-version.outputs.version }}"
        echo "VERSION_NUMBER=$VERSION_BASE" >> $GITHUB_OUTPUT

    - name: Construct tag name
      id: construct-tag
      run: |
        if [[ "${{ steps.validate-input.outputs.BUILD_TYPE }}" == "release" ]]; then
          TAG_NAME="HDFView-${{ steps.extract-version.outputs.VERSION_NUMBER }}"
        else
          TAG_NAME="snapshot"
        fi
        echo "VERSION_TAG=$TAG_NAME" >> $GITHUB_OUTPUT
        echo "Constructed tag name: $TAG_NAME"

  get-base-names:
    runs-on: ubuntu-latest
    outputs:
      hdf4-name: ${{ steps.get-hdf4-base.outputs.base-name }}
      hdf5-name: ${{ steps.get-hdf5-base.outputs.base-name }}
    steps:
      - name: Get HDF4 base name
        id: get-hdf4-base
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          BASE_NAME=$(gh release view snapshot --repo HDFGroup/hdf4 --json assets --jq '.assets[] | select(.name == "last-file.txt") | .url' | xargs curl -sL)
          echo "base-name=$BASE_NAME" >> $GITHUB_OUTPUT
          echo "HDF4 base name: $BASE_NAME"

      - name: Get HDF5 base name
        id: get-hdf5-base
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          BASE_NAME=$(gh release view snapshot --repo HDFGroup/hdf5 --json assets --jq '.assets[] | select(.name == "last-file.txt") | .url' | xargs curl -sL)
          echo "base-name=$BASE_NAME" >> $GITHUB_OUTPUT
          echo "HDF5 base name: $BASE_NAME"

  call-workflow-tarball:
    needs: determine-version
    uses: ./.github/workflows/tarball.yml
    with:
      use_environ: release

  call-workflow-maven-build:
    needs: [determine-version, get-base-names, call-workflow-tarball]
    uses: ./.github/workflows/maven-build.yml
    with:
      file_base: ${{ needs.call-workflow-tarball.outputs.file_base }}
      build_type: ${{ needs.determine-version.outputs.build_type }}
      use_hdf: ${{ needs.determine-version.outputs.hdf4_tag }}
      use_hdf_name: ${{ needs.determine-version.outputs.build_type == 'snapshot' && needs.get-base-names.outputs.hdf4-name || needs.determine-version.outputs.hdf4_tag }}
      use_hdf5: ${{ needs.determine-version.outputs.hdf5_tag }}
      use_hdf5_name: ${{ needs.determine-version.outputs.build_type == 'snapshot' && needs.get-base-names.outputs.hdf5-name || needs.determine-version.outputs.hdf5_tag }}
      snap_name: HDFView-${{ needs.call-workflow-tarball.outputs.source_base }}
      use_environ: release
      publish_to_maven_registry: true
    secrets: inherit

  call-workflow-release:
    needs: [determine-version, call-workflow-tarball, call-workflow-maven-build]
    permissions:
      contents: write # In order to allow tag creation
    uses: ./.github/workflows/release-files.yml
    with:
      file_base: HDFView-${{ needs.call-workflow-tarball.outputs.source_base }}
      file_branch: ${{ needs.call-workflow-tarball.outputs.file_branch }}
      file_sha: ${{ needs.call-workflow-tarball.outputs.file_sha }}
      use_tag: ${{ needs.determine-version.outputs.version_tag }}
      use_environ: release

